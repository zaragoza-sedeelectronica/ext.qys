<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" xmlns:sede="http://www.zaragoza.es" xAmlns:th="http://www.thymeleaf.org">

<head>
    <meta charset="utf-8" />
    <sede:meta title="Convenios" description="Convenios del Ayuntamiento de Zaragoza" author="Ayuntamiento de Zaragoza" keywords="sede,web,convenios"></sede:meta>
</head>

<body>
    <sede:content>
        <!--/*<div th:if="${#lists.contains(attr_peticion.permisosEnSeccion, 'NEW')}" class="pull-right">
            <a href="./new" class="btn btn-primary">Nuevo registro</a>
        </div>
        <div class="row">
            <div class="col-md-6">
                <h2>Líneas Estratégicas</h2>
                <ul>
                    <li th:each="doc: ${lineas.body.result}">
                        <a th:href="@{'list'(lineaEstrategica = ${doc.title}, ejercicio = ${doc.ejercicio})}">
                            <th:block th:text="${doc.title}" />
                        </a> Año:
                        <th:block th:text="${doc.ejercicio}" />
                        <span class="label label-danger" th:if="not ${doc.title}">SIN title</span>
                    </li>
                </ul>
            </div>
            <div class="col-md-6">
                <h2>Servicios municipales</h2>
                <ul>
                    <li th:each="doc: ${estructuras.body.result}">
                        <a th:href="@{'list'(entidad.id = ${doc.id})}">
                            <th:block th:text="${doc.title}" />
                        </a>
                        <span class="label label-danger" th:if="not ${doc.id}">SIN ID DE ENTIDAD</span>
                    </li>
                </ul>
            </div>
        </div>*/-->
    </sede:content>
</body>

</html>
